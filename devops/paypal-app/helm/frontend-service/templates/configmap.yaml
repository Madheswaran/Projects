apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-service-config
data:
  APP_NAME: {{ .Values.config.appName | quote }}
  ENV_NAME: {{ .Values.config.environment | quote }}
  DB_HOST: {{ .Values.config.db_host | quote }}